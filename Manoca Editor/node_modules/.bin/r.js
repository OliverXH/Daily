#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_requirejs@2.3.6@requirejs/bin/r.js" "$@"
  ret=$?
else 
  node  "$basedir/../_requirejs@2.3.6@requirejs/bin/r.js" "$@"
  ret=$?
fi
exit $ret
