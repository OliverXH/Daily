#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_require@2.4.20@require/bin/require-command.js" "$@"
  ret=$?
else 
  node  "$basedir/../_require@2.4.20@require/bin/require-command.js" "$@"
  ret=$?
fi
exit $ret
